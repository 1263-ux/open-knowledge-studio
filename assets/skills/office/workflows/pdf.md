# PDF workflow

Use the host `pdf` workflow for production PDF work or a controlled conversion
from an editable source. Preserve the editable source whenever both formats
are requested. If neither mature route is available, report
`environment_limited`.

Render every page and inspect glyphs, clipping, page count, tables, and
selectable text. A successful conversion does not prove layout survived.
