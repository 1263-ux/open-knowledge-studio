# PPT Master Adapter

PPT Master is an optional production enhancement for high-design, native,
template-sensitive decks. It receives the already validated outline/evidence
package rather than selecting facts itself. Probe it locally, record its
version, preserve its editable PPTX output, then render and inspect every
slide before delivery.
