# Excel Native Adapter

Use a native Excel Adapter only for features that ordinary XLSX libraries do
not faithfully control, such as Power Query, Data Model, native PivotTables,
VBA, or interaction with an open workbook. Require its documented Windows and
desktop-Excel environment probe before use. Prefer its compact CLI surface for
agent automation; record the Adapter version and workbook target in delivery
metadata.
