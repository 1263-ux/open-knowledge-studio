# dsh-oks / Open Knowledge Studio acceptance record

Date: 2026-08-19
Design version: 3
Plan ID: dsh-oks-ac-20260819-v3
State: VERIFY / TRIAL; overall delivery is not complete

This record is evidence-first. It distinguishes runtime evidence, Owner-only visual gates, human-review gates, and external actions that were not performed.

## Local verification actually run

Working tree: `<dsh-oks-worktree>`

```text
npm.cmd test
5 passed, 0 failed

npm.cmd run build
PASS; tsdown emitted only known deprecation warnings

node --experimental-strip-types --input-type=module -e "await import('./src/index.ts'); console.log('HOST_IMPORT_OK')"
HOST_IMPORT_OK

git diff --check
PASS
```

OKS CLI commands, run from the configured knowledge base:

```text
oks status                    PASS
oks config show               PASS
oks recall --format json      PASS
```

The configured knowledge base is `<oks-instance>`; `settings/recall.yaml` is restored to `recall.floor: 0.7` and the user-level OKS config points to that path.

## Runtime evidence

Controlled DSH Web runtime:

```text
URL: http://127.0.0.1:3080
Process: controlled acceptance runtime
Session: session-a4263541-e021-4ee1-9c61-6cc008b9cf8e
```

`POST /api/settings.describe` returned:

```text
result.ok=true
result.value.writable=true
result.value.hasDocument=true
namespaces=12
oks namespace count=1
```

The `oks` value resolved to these 12 concrete fields:

```text
knowledge_base_path
recall_floor
recall_topn
recall_minlen
recall_cooldown
prestep_floor
prestep_knowledge_only
posttool_mode
posttool_floor
posttool_topn
posttool_signal_rel_floor
search_backend
```

The runtime record with tool, hook, Skill, and settings writeback traces is:
`records/acceptance/dsh-oks-runtime-20260819.md`.

## Acceptance matrix

| AC | State | Evidence / boundary |
|---|---|---|
| AC1 | PASS | `settings.describe` exposes `oks`, `writable=true`, and `hasDocument=true`; no dsh-oks load error observed in the controlled runtime evidence. |
| AC2 API | PASS | The API returns a real schema/value with the 12 fields above. |
| AC2 GUI | OWNER PENDING | Owner must click `OKS settings entry -> OKS card` and confirm it is non-blank, not `unavailable`, and shows the fields. API evidence is not a substitute. |
| AC3 tools | PASS | Six tools were catalogued and executed with non-error results: `oks_recall`, `oks_status`, `oks_wiki_use`, `oks_metrics`, `oks_inject_stats`, `oks_inject_feedback`. |
| AC3 hooks | PASS with runtime evidence | `agent/pre-step` injected `<recalled-memory source="oks">`; post-tool feedback/signal path executed `oks_inject_feedback` successfully. |
| AC3 Skill | PASS | `skill.list` returned `oks-recall`, `modelInvocable=true`. |
| AC4 | PASS | Reversible live cycle `0.70 -> 0.65 -> 0.70` updated DSH settings, `settings/recall.yaml`, and `~/.oks/config.json`; refresh/describe persistence and restoration were verified. |
| AC5 | PASS | `.agent/DESIGN.md`, `.agent/PLAN.md`, `.agent/STATUS.md`, `.agent/CURRENT_STATE`, and `.agent/RESULT.md` exist and preserve partial/pending states. |
| AC6 | PASS | `oks status`, `oks config show`, and `oks recall --format json` succeeded. |
| AC7 | PARTIAL | Real Bilibili source, raw bundle, and Kimi draft exist. Human promotion, recall of the promoted page, final Ki3 output, and before/after screenshots remain missing. |
| AC8 | LOCAL DRAFT / CLOUD PENDING | `dsh-oks/skills/oks-case-init/SKILL.md` contains initialization, inputs, outputs, call procedure, and guardrails. No cloud publication was performed. |
| AC9 | LOCAL MATERIAL / TEAM SIGNOFF PENDING | `dsh-oks/docs/team-ingestion-guide.md` covers GitHub, articles, AI Native, Agent content, organization, recall, and feedback. No team-owner signoff is recorded. |
| AC10 | LOCAL DRAFT / EXTERNAL PENDING | Install prompt, README usage notes, case material, promotion material, and Windows/rc.6 notes exist locally. No external publication or announcement was performed. |
| AC11 | LOCAL UNCOMMITTED / EXTERNAL PENDING | Source, build artifacts, docs, and PR draft exist locally. No fork, push, PR, release, deployment, or package publication was performed. |

## AC7 case evidence boundary

Real:

- Source: `https://www.bilibili.com/video/BV1qg3F6dEvm/`
- Raw bundle: `raw/2026/08/08/agent-capture/bundle-b70c3666e8347e6f`
- Draft: `drafts/kimi-k3-review-bilibili.md`
- Case procedure: `dsh-oks/docs/cases/kimi-k3-to-ki3.md`
- Runtime `oks_recall` execution returned non-error mixed knowledge/episodic results.

Not yet real/completed:

- human review and promotion of the Kimi draft into `wiki/`;
- a recall result containing that promoted Kimi page;
- a final Ki3 plan generated from that recalled page;
- before/after screenshots or recording.

The case document is therefore a local delivery draft, not a completed case claim.

## Owner and external gates

1. Owner visually confirms the AC2 GUI card and fields.
2. Owner explicitly approves or edits the Kimi draft before promotion; do not auto-promote.
3. Fork, push, PR, release, publication, deployment, and external messages remain separate actions requiring exact authorization for each action.

## Rollback

- Restore settings through the DSH settings API and verify both `settings/recall.yaml` and `~/.oks/config.json`.
- Stop only temporary test instances after collecting evidence; identify the controlled runtime by verified process metadata rather than a stale PID.
- Revert source changes only through a reviewed Git operation; never delete user configuration as rollback.
