# Pilot 00 — G4 Protocol Baseline

- `status`: complete
- `classification`: real CLI integration baseline, not a P3 long-task sample
- `date`: 2026-09-06
- `isolated_root`: `C:\Users\chenfeng\AppData\Local\Temp\oks-mail-p2-verify-20260906-g4`

## Observed flow

Claude handoff → Codex Session A presented → duplicate wait suppressed → Codex Session B
presented and acknowledged → same-Thread reply → Claude presented and acknowledged →
recipient archive → fresh Session wait timed out.

Concurrent same-Thread replies remained canonical, visible, and chronologically ordered.
Adversarial Mail content remained escaped inside the untrusted Hook envelope.

## Metrics

| Metric | Result |
| --- | --- |
| `handoff_success_rate` | 1/1 protocol flow completed |
| `context_reask_count` | not measured as a long task |
| `human_intervention_count` | 0 in the direct CLI sequence |
| `duplicate_work_count` | 0; same-Session duplicate suppressed |
| `handoff_to_action_latency` | not recorded |
| `unresolved_thread_count` | 0 after archive |

## Limitation

Claude headless Hook parsing and `additionalContext` delivery passed, but the non-interactive
model did not report or act on the hidden reminder. This is a model-consumption/host behavior
finding, not evidence that Core delivery failed. It remains a P3 host-compatibility item.

