[pytest]
# Build artifacts mirror scripts/ into cli/, so pytest either collects duplicate
# test basenames or imports the stale copy of a module (seen as
# "cannot import name '_fsync_dir' from '_shared'"). Keep collection on source.
norecursedirs = cli/oks_connector cli/scripts cli/build cli/knowledge_studio/_assets .git .venv venv node_modules docs/_site examples
